# Backend (placeholder)

- Entry: `src/index.js` currently serves a minimal HTTP server so the folder is runnable without extra deps.
- Replace with your real API stack (Express/Fastify/Nest/etc). Add deps here and update scripts as needed.
- Env vars: create `backend/.env` and set things like `PORT=4000`, `MONGODB_URI=...`.
- Start locally: `npm run dev --prefix backend` (or `npm start --prefix backend`).
